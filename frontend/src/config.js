// Single place to configure where the backend API lives.
// Local dev: FastAPI runs on port 8000.
// When you deploy the backend (Render/Railway), change this to that URL.
export const API_BASE_URL = "https://email-header-analyzer-api.onrender.com";